import { OrganisationpipePipe } from './organisationpipe.pipe';

describe('OrganisationpipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganisationpipePipe();
    expect(pipe).toBeTruthy();
  });
});
