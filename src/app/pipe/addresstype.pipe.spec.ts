import { AddresstypePipe } from './addresstype.pipe';

describe('AddresstypePipe', () => {
  it('create an instance', () => {
    const pipe = new AddresstypePipe();
    expect(pipe).toBeTruthy();
  });
});
